<Window x:Class="SignProgrammer.GraphicEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SignProgrammer"
        xmlns:controls="clr-namespace:SignProgrammer"
        mc:Ignorable="d"
        Title="GraphicEditor" Height="300" Width="600">
    <Grid>
        <ListBox x:Name="fileBox" HorizontalAlignment="Left" Width="100">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="{Binding FileName}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox x:Name="listBox" Height="100" Width="100"/>
        </ListBox>
        <controls:GraphicEditorControl Margin="100,0,0,0" />
    </Grid>
</Window>
